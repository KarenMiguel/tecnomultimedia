class Juego {
  //Se llama la clase Portacosméticos
  // declaro el arreglo de la clase objeto
  // declaro el arreglo de la clase Zapatos
  // contador de agarrar y vidas
  // contador para que caigan los maquillajes y zapatos cada 1 y 2 segundos respectivamente.
  Juego() {
    //inicio la clase portacosméticos 
    for (int i=0; i<10; i++) {
      // inicio el objeto maquillaje
    }
    for (int i=0; i<5; i++) {
      // dibuja los zapatos
    }
  }
  void Dibujar() {
    //Función dibujar del portacosméticos
    /*Con frameCount modulo de 60 = 0 se llama el número de los maquillajes
     con frameCount modulo de 120 = 0 se llama el número de los zapatos 
     */
    //Función de maquillaje dibujar
    //Función de zapato dibujar
  }
  void Agarrar() {
    for (int i=0; i<10; i++) {
      //Función de objeto agarrar llamando las variables de portacosméticos x, y e tam
      //variable agarrar suma
    }
  }
  void Perder() {
    for (int i=0; i<10; i++) {
      //Función de maquillaje perder llamando la variable de portacosméticos y 
      //Función de zapatos perder llamando las variables de portacosméticos x, y e tam
      //vidas resta
    }
  }
}
